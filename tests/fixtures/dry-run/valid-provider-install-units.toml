[targets.machine]
platform = { os = "linux" }

[targets.machine.providers.brew]
activate = { variables = { BREW = "${env:ROOT}/homebrew/bin/brew" } }
probe = { program = "${env:BREW}", args = ["--version"] }
ensure = { program = "bootstrap-brew" }
install = { program = "${env:BREW}", args = ["before", "${package:provider_args}", "middle", "${package:names}", "after"] }

[targets.machine.packages]
alpha = { provider = "brew" }
beta = { provider = "brew" }
fonts = { provider = "brew", names = ["font-one", "font-two"], provider_args = ["--cask"] }
