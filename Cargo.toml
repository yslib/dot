[package]
name = "dot"
version = "0.0.2"
edition = "2024"

[features]
dev-platform-override = []

[dependencies]
clap = { version = "4.6", features = ["derive"] }
comfy-table = "7.2"
directories = "6.0"
serde = { version = "1.0", features = ["derive"] }
toml = "1.1"
