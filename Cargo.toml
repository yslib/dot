[package]
name = "dot"
version = "0.1.0"
edition = "2024"
license = "MIT"

[features]
dev-platform-override = []

[dependencies]
clap = { version = "4.6", features = ["derive"] }
comfy-table = "7.2"
directories = "6.0"
serde = { version = "1.0", features = ["derive"] }
thiserror = "2"
toml = "1.1"
url = "2.5"
